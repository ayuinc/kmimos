<div class="container-sm">
  <div class="row text-center mt-42">
    <%= link_to image_tag('huella-mensaje-17.png' ), root_url %>
      <h1 class="mb-21">¡Gracias por hacer tu reserva!</h1>
      <h3>Los detalles de tu reserva a continuación:</h3>
    <div class="col-md-4"></div>
    <div class="col-md-5">
      
      <div class="text-left">
        <% if !session[:start_date].nil? %>
          <h4 class="text-gray"><small class="text-black bold">Check-in:</small> <%= session[:start_date].strftime("%d/%m/%Y") %>
        <% end %></h4>
        <% if !session[:end_date].nil? %>
          <h4 class="text-gray"><small class="text-black bold">Check-out:</small> <%= session[:end_date].strftime("%d/%m/%Y") %></h4>
        <% end %>
        <% if !session[:chosen_provider_id].nil? %>
      <h4 class="text-gray"><small class="text-black bold">Cuidador:</small> <%= Provider.find(session[:chosen_provider_id]).name %> <%= Provider.find(session[:chosen_provider_id]).last_name_1 %> <%= Provider.find(session[:chosen_provider_id]).last_name_2 %></h4>
  <h4 class="text-gray"><small class="text-black bold">Precio:</small> Mex $<%= Provider.find(session[:chosen_provider_id]).price %> / noche</h4>
        <% end %>
      </div>
    </div>
    <div class="row">
      <div class="col-md-12 text-center">
        <h3>Para tu comodidad hemos enviado la información a:</h3>
      
        <% unless session[:user_email].nil? %>
        <h4><%= session[:user_email] %></h4>
      <% end %>
        <hr class="mt-42">
      </div>
    </div>
  </div>
</div>


<!-- Facebook Conversion Code for Reservacion exitosa -->
<script>
(function() {
  var _fbq = window._fbq || (window._fbq = []);
  if (!_fbq.loaded) {
    var fbds = document.createElement('script');
    fbds.async = true;
    fbds.src = '//connect.facebook.net/en_US/fbds.js';
    var s = document.getElementsByTagName('script')[0];
    s.parentNode.insertBefore(fbds, s);
    _fbq.loaded = true;
  }
})();
window._fbq = window._fbq || [];
window._fbq.push(['track', '6021903685305', {'value':'0.01','currency':'MXN'}]);
</script>
<noscript>
  <img height="1" width="1" alt="" style="display:none" src="https://www.facebook.com/tr?ev=6021903685305&amp;cd[value]=0.01&amp;cd[currency]=MXN&amp;noscript=1" />
</noscript>