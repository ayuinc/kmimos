%center
  = render "layouts/shared/flash_alerts"
.container.mh-630
  .row.mt-70
    .col-md-4
    .col-md-4 
      .panel.panel-primary
        .panel-heading.medium.text-gray.mb-14 
          %center 
            %h5 Ingresa a tu cuenta de cuidador Kmimos
        .panel-body
          - @provider = Provider.new(email: "#{params[:provider][:email] rescue ''}")
          = simple_form_for(@provider, url: session_path(@provider)) do |f|
            .form-group= f.input :email 
            .form-group= f.input  :password, label: "Contraseña"
            = f.submit "Ingresa", class: 'form-control btn btn-primary mb-21'
          = render "providers/shared/links"
    .col-md-4
    
